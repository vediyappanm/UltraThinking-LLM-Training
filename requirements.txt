# Core PyTorch
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Transformers & Tokenization
transformers>=4.35.0
datasets>=2.14.0
tokenizers>=0.14.0
tiktoken>=0.4.0
sentencepiece>=0.1.99

# Utilities
accelerate>=0.24.0
einops>=0.7.0
pyarrow>=14.0.0
Pillow>=10.0.0
opencv-python>=4.8.0
librosa>=0.10.0
soundfile>=0.12.0
nltk>=3.8.0
rouge-score>=0.1.2
sacrebleu>=2.3.0
scikit-learn>=1.3.0
scipy>=1.11.0
detoxify>=0.5.0
astunparse>=1.6.3

# Code Quality / Dev Tools
black>=23.0.0
pylint>=2.17.0
flake8>=7.3.0
mypy>=1.5.0
pre-commit>=3.4.0
bandit>=1.7.5
python-dotenv>=1.0.0

# Experiment Tracking
mlflow>=2.12.0
tensorboard>=2.14.0
tqdm>=4.66.0
rich>=13.5.0

# Config / Hydra
hydra-core>=1.3.0
omegaconf>=2.3.0
pyyaml>=6.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-timeout>=2.1.0
pytest-xdist>=3.3.0

# Optimization / Hyperparameter Tuning
optuna>=3.3.0
# ray[tune]==2.6.0  # Optional. No wheels for Python 3.13 on Windows. Install a newer Ray version once it supports your Python, or skip.

# Data / Visualization
numpy>=1.24.0
pandas>=2.1.0
matplotlib>=3.7.0
seaborn>=0.12.0
jupyter>=1.0.0
ipywidgets>=8.1.0

# System / GPU Stats
psutil>=5.9.0
# gpustat>=1.1.0    # Optional GPU monitor; skip on Windows/Python 3.13
# py3nvml>=0.2.7    # Optional NVIDIA NVML bindings; skip on Windows/Python 3.13

# Web / APIs
fastapi>=0.103.0
uvicorn>=0.23.0
gradio>=3.45.0
streamlit>=1.27.0
huggingface-hub>=0.15.0

# ============= UltraThinking v2.0 Dependencies =============

# Parameter-Efficient Fine-Tuning (LoRA, QLoRA, DoRA)
peft>=0.7.0
bitsandbytes>=0.41.0  # For 4-bit/8-bit quantization

# Quantization & Optimization
auto-gptq>=0.5.0  # GPTQ quantization
autoawq>=0.1.6  # AWQ quantization

# Evaluation & Benchmarking
lm-eval>=0.4.0  # Language model evaluation harness

# Advanced Training (DPO, ORPO, etc.)
trl>=0.7.4  # Transformer Reinforcement Learning (includes DPO)

# Flash Attention (optional, for speed)
# flash-attn>=2.3.0  # Uncomment if CUDA available

# Distributed Training
deepspeed>=0.12.0  # Advanced distributed training

# Cloud SDKs (optional)
boto3>=1.28.0
google-cloud-storage>=2.13.0
azure-storage-blob>=12.19.0

# Security & Compliance (optional)
cryptography>=41.0.0
opacus>=1.4.0

# AutoML / NAS
optuna>=3.4.0

# ONNX export (optional)
onnx>=1.14.0
